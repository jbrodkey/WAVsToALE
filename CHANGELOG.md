# Changelog

## 1.0.1 — 2025-12-02
- GUI: Checkbox label clarified to “Infer UCS category/subcategory if filename lacks a UCS ID”.
- CLI: Added aliases `--ucs-inference` and `--no-ucs-inference` (legacy flags `--allow-ucs-guess` / `--no-ucs-guess` still supported).
- Packaging: No changes required; UCS CSV remains bundled under `data/`.

## 1.0.0 — 2025-11-XX
- Initial packaged release for WAVsToALE (ALE generation from WAV metadata with UCS support).
